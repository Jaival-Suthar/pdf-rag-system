# Evaluation dataset notes

This directory currently contains a small scaffold/example evaluation set rather than a validated benchmark corpus.
The questions here exercise the retrieval and generation flow using broad, fixture-derived prompts; `expected_doc_id` is `null` because document IDs are generated when PDFs are ingested.
It is not a validated benchmark corpus, and scores produced from this dataset should not be treated as representative retrieval-quality metrics.
